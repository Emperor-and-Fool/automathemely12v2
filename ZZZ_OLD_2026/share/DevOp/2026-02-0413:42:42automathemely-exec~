#!/usr/bin/env bash
set -euo pipefail

REPO="$HOME/TheCave/_GitProjects/automathemely-repo"
VENV="$HOME/Pysolated/penv_automathemely12v2"

source "$VENV/bin/activate"
cd "$REPO"
export PYTHONPATH="$REPO"

exec python -m bin.run "$@"